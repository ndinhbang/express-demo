version: '3.9'

services:
  backend:
    image: backend:v1
    build:
      context: .
      dockerfile: Dockerfile.prod
      target: prod
    volumes:
      - backend:/app
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: production
volumes:
  backend:
